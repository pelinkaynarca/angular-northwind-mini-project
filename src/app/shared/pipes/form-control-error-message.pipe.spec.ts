import { FormControlErrorMessagePipePipe } from './form-control-error-message.pipe';

describe('FormControlErrorMessagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlErrorMessagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
